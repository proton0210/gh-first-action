name: First workflow

on: workflow_dispatch

jobs:
  #job name
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo "Hello World!"
          echo "Kayden Kross"
      - name: print goodBye
        run: echo "Good Bye"
